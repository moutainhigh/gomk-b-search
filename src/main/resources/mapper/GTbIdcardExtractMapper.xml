<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.gomk.mapper.GTbIdcardExtractMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="io.gomk.model.GTbIdcardExtract">
        <id column="id" property="id" />
        <result column="prj_code" property="prjCode" />
        <result column="prj_name" property="prjName" />
        <result column="idcard_name" property="idcardName" />
        <result column="idcard_number" property="idcardNumber" />
        <result column="uuid" property="uuid" />
        <result column="title" property="title" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, prj_code, prj_name, idcard_name, idcard_number, uuid, title
    </sql>

</mapper>
