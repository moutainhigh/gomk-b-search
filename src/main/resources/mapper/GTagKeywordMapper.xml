<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.gomk.mapper.GTagKeywordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="io.gomk.model.GTagKeyword">
        <result column="tag_id" property="tagId" />
        <result column="must_all" property="mustAll" />
        <result column="must_full" property="mustFull" />
        <result column="anyone" property="anyone" />
        <result column="must_no" property="mustNo" />
        <result column="date_range" property="dateRange" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        tag_id, must_all, must_full, anyone, must_no, date_range
    </sql>

</mapper>
